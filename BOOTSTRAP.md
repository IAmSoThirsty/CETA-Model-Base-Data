# CETA repository bootstrap

This temporary file initializes the empty repository so the sealed CETA Epoch-Ready Reference v0.3.0 tree can be materialized in one canonical import commit. It is removed automatically by the import workflow.
